{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.8.3"
  },

  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  }
}
